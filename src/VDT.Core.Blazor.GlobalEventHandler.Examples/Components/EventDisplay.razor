@using System.Text.Json
@typeparam TEventArgs

@if (EventArgs != null) {
    <pre class="bg-light p-2">
        @JsonSerializer.Serialize(EventArgs, new JsonSerializerOptions() { WriteIndented = true })
    </pre>
}

@code {
    [Parameter]
    public TEventArgs? EventArgs { get; set; }
}
